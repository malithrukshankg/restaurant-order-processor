# restaurant-order-processor
A React + TypeScript based restaurant ordering interface that allows customers to select menu items, calculates totals with GST included, and displays a formatted receipt. Includes well-structured code and unit tests.
